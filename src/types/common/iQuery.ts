export interface ISearchQuery {
  searchText: string;
  dateRange: string[];
  page: number | string;
  nPerPage: number;
}

export interface IFilterQuery {
  searchText: string;
  dateRange: string[];
  page: number | string;
  nPerPage: number;
}
